<div class="flex flex-col items-center justify-center py-16 px-4 bg-white rounded-xl shadow-md w-full">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-gray-300 mb-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 17l4 4 4-4m0-5V3a1 1 0 00-1-1H5a1 1 0 00-1 1v14a1 1 0 001 1h3" />
    </svg>
    <h3 class="text-2xl font-semibold text-gray-700 mb-2">No Cards Found</h3>
    <p class="text-gray-500 mb-6 text-center max-w-md">There are currently no cards to display. Run the classification agent to rebuild the list.</p>
    <button @onclick="LoadData" class="bg-blue-600 text-white px-6 py-2 rounded-full shadow hover:bg-blue-700 transition">Run Agent</button>
</div>

@code {
    [Parameter]
    public EventCallback OnLoad { get; set; }

    private void LoadData()
    {
        // Logic to load data or trigger an event
        OnLoad.InvokeAsync();
    }
}