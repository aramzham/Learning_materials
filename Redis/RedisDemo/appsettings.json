{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
  // connection strings is commented as it's already written in secrets.json file
  // secrets.json overrides every matching property in appsettings.json
  //"ConnectionStrings": {
  //  "Redis": "localhost:6379"
  //}
}
